# deckard

An Android AI-slop detector: a floating "Deckard" mascot reads the text on your current screen
(on-device screenshot OCR via Gemma/LiteRT-LM) and is built to judge whether that content is
AI-generated "slop".
